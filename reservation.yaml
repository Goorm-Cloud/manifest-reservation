apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoochacha-server
  namespace: zinucha
spec:
  replicas: 1  # 서버는 확장 가능하도록 설정
  selector:
    matchLabels:
      app: zoochacha-server
  template:
    metadata:
      labels:
        app: zoochacha-server
    spec:
      containers:
        - name: zoochacha-server
          image: 651706756261.dkr.ecr.ap-northeast-2.amazonaws.com/reservation:latest
          imagePullPolicy: Always
          env:
            - name: DB_HOST
              value: "mysql.zoochacha-db.svc.cluster.local"
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: "zoochacha"
            - name: DB_USER
              value: "root"
            - name: DB_PASSWORD
              value: ""
          ports:
            - containerPort: 8003
---
apiVersion: v1
kind: Service
metadata:
  name: zoochacha-server
  namespace: zinucha
spec:
  selector:
    app: zoochacha-server
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
